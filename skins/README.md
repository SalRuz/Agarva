# Skins

Положи сюда картинки скинов:
- `.png`, `.jpg`, `.jpeg`, `.webp`, `.gif`, `.svg`

После добавления файлов перезапусти `npm run dev` (или сборку), чтобы они появились в меню «Скины».

Путь папки: корень проекта → `skins/`
(рядом с `src/`, `shared/`, `package.json`)
